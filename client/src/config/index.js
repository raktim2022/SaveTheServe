/**\n * Configuration for environment variables and app settings\n */\n\nexport const config = {\n  // API Configuration\n  api: {\n    baseUrl: process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3000/api',\n    timeout: 30000,\n  },\n  \n  // Site Configuration\n  site: {\n    name: 'SaveTheServe',\n    url: process.env.NEXT_PUBLIC_SITE_URL || 'https://savetheserve.com',\n    description: 'Food rescue platform connecting restaurants and NGOs to reduce waste and fight hunger',\n  },\n  \n  // Map Configuration\n  maps: {\n    mapboxToken: process.env.NEXT_PUBLIC_MAPBOX_TOKEN,\n    defaultCenter: [77.2090, 28.6139], // New Delhi coordinates\n    defaultZoom: 12,\n  },\n  \n  // Feature Flags\n  features: {\n    enableAnalytics: process.env.NODE_ENV === 'production',\n    enableSentry: process.env.NODE_ENV === 'production',\n    enablePWA: process.env.NEXT_PUBLIC_ENABLE_PWA === 'true',\n    enableNotifications: process.env.NEXT_PUBLIC_ENABLE_NOTIFICATIONS === 'true',\n  },\n  \n  // App Limits\n  limits: {\n    maxFileSize: 5 * 1024 * 1024, // 5MB\n    maxImageSize: 2 * 1024 * 1024, // 2MB\n    maxFoodListings: 10,\n    maxRequests: 5,\n  },\n  \n  // Cache Configuration\n  cache: {\n    defaultTTL: 5 * 60 * 1000, // 5 minutes\n    longTTL: 24 * 60 * 60 * 1000, // 24 hours\n  },\n};\n\nexport default config;